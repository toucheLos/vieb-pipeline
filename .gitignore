venv
work/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# The odd-one-out contact sheets: 400 MB of scoring workspace, regenerated by
# `island_look.py --build` from the sealed key. The responses they produced are
# in results/island_look.json and the clips they were cut from are committed.
results/island_look/trial_*.png

# The annotation clips: 42 MB of all-intra video, regenerated deterministically
# by `annot_sample.py` from the committed manifest (seed 0). They are uploaded
# to the annotation tool's asset store, not served from here.
results/annot/clips/
