"""Pixel-domain motion: the arena's own noise floor, and an ezTrack-family
freeze score derived against it.

Registered in `results/PIXEL_PREREGISTRATION.md`. The algorithm was verified
against ezTrack's source (`github.com/DeniseCaiLab/ezTrack`, `master`,
`FreezeAnalysis/FreezeAnalysis_Functions.py`); **ezTrack is not installed** and
everything here is named a reimplementation.
"""
