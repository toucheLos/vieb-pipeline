# Pinned to the interpreter the cluster module provides: python/3.11.4.
#
# Identical to recur's and shapeflow's pins, deliberately: this repo consumes
# float32 arrays those two produced, and a numeric library that differs between
# the producer and the consumer of an array is a difference nobody would think
# to look for.
numpy==2.4.6
scipy==1.17.1
pandas==3.0.5
tables==3.11.1
h5py==3.15.1
scikit-learn==1.9.0
torch==2.13.0
pytest==9.1.1
matplotlib==3.11.1
opencv-python-headless==5.0.0.93  # frame READING only; it cannot write H.264 here
cairosvg==2.9.0

# `mypy --strict` over vieb/tok and vieb/qc (see mypy.ini for why the surface is
# narrow). scipy-stubs is not optional -- without it the partial-correlation code
# is an untyped import and strict mode refuses.
mypy==1.19.0
scipy-stubs==1.17.1.5

# Phase B, the cleaning bakeoff. Called as a library, never vendored.
# movement==0.14.0


# SAM, for the pixel pilot's OPTIONAL mask-feature arm. OFF THE CRITICAL PATH:
# no result depends on it, and nothing is refused if it is absent.
#
# Both installed with --no-deps, deliberately. `segment-anything` and
# `torchvision` both name `torch` and resolving their pins would have been free
# to REPLACE torch 2.13.0+cu130 -- and this venv is a symlink shared with
# `~/recur`, so that would have silently re-based every GPU result in two repos.
# torchvision 0.28.0 is the build that pairs with torch 2.13, taken from the
# cu130 index so the CUDA majors match. Verified after installing, on an A100
# and not on the login node: torch 2.13.0+cu130, cuda True, a CUDA matmul, and
# torchvision.ops importable.
#
# The SAM checkpoint is NOT vendored and NOT downloaded here.
segment-anything==1.0
torchvision==0.28.0  # +cu130 build; --index-url https://download.pytorch.org/whl/cu130
